{
  "name": "pi-remembrall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pi Remembrall: lightweight persistent memory extension for Pi agents.",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {},
  "devDependencies": {}
}
